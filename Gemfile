source 'https://rubygems.org'

group :test do
  gem 'coveralls', require: false
  gem 'pry'
  gem 'rspec'
end

# Ruby 2.0 and 2.1 support was dropped in rubocop 0.51
# Upgrade this if/when the gem drops support for ruby version < 2.2
gem 'rubocop', '~>0.50.0'

# Specify your gem's dependencies in regexp-examples.gemspec
gemspec
